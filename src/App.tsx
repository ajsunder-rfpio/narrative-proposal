// Placeholder shell only. The real screens (intake → outline → themes →
// drafting → review → export) are defined in docs/design/ and land later.
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-semibold tracking-tight">
        Narrative Proposal
      </h1>
    </main>
  );
}
